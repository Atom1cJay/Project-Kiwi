using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaypointRandomizer : MonoBehaviour
{

    //Get a random waypoint within the given radius
    Vector2 RandomWaypoint(float radius)
    {
        return Vector2.zero;
    }

    //Get a random waypoint within the given radius relative to the worldPos
    Vector2 RandomWaypoint(float radius, Vector2 worldPos)
    {
        return Vector2.zero;
    }
}
